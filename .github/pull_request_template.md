## What changes

## Why

## Checks

- [ ] `npm run verify` passes
- [ ] Tests added or updated for changed logic
- [ ] No secrets, `.env` files, or personal mailbox data
- [ ] Gmail selectors stay in `packages/gmail/src/selectors.ts`
- [ ] Nothing sends email without the user confirming it
- [ ] Stored names (IndexedDB, `chrome.storage` keys, tracking protocol IDs) are unchanged, or a migration is included
