{
  "name": "gmail-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Gmail intelligence Chrome extension — local AI inbox + email open tracking. No Gmail API.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*",
    "workers/*"
  ],
  "scripts": {
    "build": "npm run build -w @gi/shared && npm run build -w @gi/gmail && npm run build -w @gi/mailbox && npm run build -w @gi/ai && npm run build -w @gi/search && npm run build -w @gi/agent && npm run build -w @gi/tracking && npm run build -w @gi/extension && npm run build -w @gi/tracker",
    "dev": "npm run dev -w @gi/extension",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint \"packages/**/*.{ts,tsx}\" \"apps/**/*.{ts,tsx}\" \"workers/**/*.{ts,tsx}\" --max-warnings 50",
    "clean": "rimraf packages/*/dist apps/*/dist workers/*/dist coverage .tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript-eslint/eslint-plugin": "^8.26.1",
    "@typescript-eslint/parser": "^8.26.1",
    "eslint": "^8.57.1",
    "eslint-plugin-react": "^7.37.4",
    "eslint-plugin-react-hooks": "^5.2.0",
    "jsdom": "^26.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.9.2"
}
